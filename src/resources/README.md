# Resources

Static media assets used by the app should live in this directory.

- Put images in `src/resources/images/`
- Put other media (audio/video/etc.) in `src/resources/media/`

Import assets from `src/resources` in React components when they are part of the app bundle.
